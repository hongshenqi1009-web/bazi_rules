# PROJECT STATUS

- 项目：五行城市
- 仓库：`hongshenqi1009-web/bazi_rules`
- 最后更新：2026-09-11
- 当前阶段：T-004 固定 100 城事实快照完成；映射 v0.1 校准审计
- 状态：进行中
- 本文件负责人：每次完成任务的 Work 执行者

## 当前目标

锁定固定 100 城的可追溯事实快照，完成 v0.1 五行向量、系统偏差与敏感性检查；在进入产品匹配前解决火/金无最高维度及余弦相似度压缩问题。

## 最近完成

- 建立项目目录骨架。
- 建立三端角色分工和标准工作流。
- 建立正式规则与候选研究的审核隔离。
- 建立状态、任务和决策台账。
- 完成三个固定候选提交的 README、许可证、核心源码、测试和算法入口审计；原生测试记录为 `lunar-javascript` 296 项、`zhiji-bazi` 15 项、`tianji` 140 项通过。
- Chat 批准 D-005 至 D-008：候选底座分工、真太阳时默认行为、大运时间精度及争议解释输出结构。
- 建立 T-003 第一阶段测试数据，加入节气分钟边界、IANA 时区、DST gap/fold、海外出生、晚子时、真太阳时跨时辰及大运候选案例。
- Chat 批准 D-009 至 D-012：23:00 子初换日、大运传统离散时辰法、USNO + GeoNames + IANA 真太阳时方案，以及稳定多轨解释框架。
- T-003 收尾：测试集由 9 例扩至 15 例，新增惊蛰边界、22:59/23:00 日界、大运方向矩阵与精确起运，以及重庆/北京/上海/伦敦四个真太阳时回归。8 个独立金标、7 个产品口径金标均通过或部分通过。
- 确定性底座已达到娱乐型 MVP 所需稳定度；T-003 标记 Done。
- Chat 批准 D-013：首批约 50 个代表性城市、五维连续向量、自然地理为主、现代城市功能为辅、引擎分离及娱乐型前台输出。
- 完成 T-004 方法论 v0.1：建立 15 个加权事实字段、传统语义与现代设计边界、自然 85%/辅助 15% 初版权重、响应矩阵、归一化、缺失值、置信度、匹配公式和版本机制。
- 用北京、上海、深圳、伦敦、纽约、洛杉矶、东京、新加坡完成 8 城压力测试；未把 demo 定性输入写入正式城市数据。
- Chat 批准 D-014：T-004 校准与审计样本固定为指定 100 城，允许批量事实采集和向量压力测试，不自行替换城市、不按印象修分。
- 完成 100/100 城 GeoNames 身份、NASA POWER 1991–2020 气候、WorldCover 2021 植被/内陆水体、Copernicus DEM GLO-90 地形和 Natural Earth 海岸事实采集；全部自然字段无缺失，字段级来源、期间、单位、范围、许可、采集日、缺失与置信度已写入 `data/cities.csv`。
- 生成 `city-elements-v0.1.0` 的 100 城整数与浮点五行向量；100 个整数向量均严格合计 100，城市 ID 与坐标均完整且唯一。
- 完成总体分布、4,950 对相似度、全体最相似/最不同、中国/海外内部相似、指定城市组、16 个极端样本、系统偏差、全球覆盖及 80/20、85/15、90/10 敏感性检查。
- 审计发现：最高维度分布为木 34、火 0、土 38、金 0、水 28；原始余弦均值 0.9771。事实层可锁定，但 v0.1 映射与原始余弦不建议直接进入发布版。

## 正在进行

- T-004 等待 Chat 审核 100 城审计：是否锁定事实快照与校准样本，是否启动中心化贡献的映射 v0.2，以及辅助层和匹配距离口径。
- `city-elements-v0.1.0` 保留为可复现审计基线，不标记为发布锁定版。
- 不开发前端，不开始商业化设计，不大规模写娱乐文案。

## 下一步

1. Chat 审核并决定是否锁定 `city-facts-2026-09-11-v1` 与 `cities100-2026-09-11-v1`。
2. 若批准，建立映射 v0.2：把每字段的共同均衡基线改为中心化贡献，复核火/金与土的联合响应，并以同一 100 城重跑全部闸门。
3. 在 L1、Jensen–Shannon 与中心化余弦中选择城市/个人匹配距离；原始余弦暂不作为前台百分比。
4. 决定辅助数据缺失时采用自然 100 / 辅助 0，还是保留 15% 中性收缩；真实辅助数据出现前不做伪敏感性结论。
5. 未来实现确定性适配器时，把 T-003 的 15 个案例转成自动化断言；当前仍不开发前端。

## 待 Chat 决定

- 是否锁定 100 城事实快照与校准样本；现有数据源为 GeoNames、NASA POWER、WorldCover、Copernicus DEM 与 Natural Earth，HydroLAKES 未进入本轮数值链。
- 是否同意 v0.1 只作为审计基线，并启动中心化贡献的 v0.2。
- 辅助数据缺失期间采用自然 100 / 辅助 0，还是保留 15% 中性收缩。
- 匹配距离改用 L1、Jensen–Shannon 还是中心化余弦；原始余弦已确认压缩严重。
- NASA POWER 是否足以支撑娱乐型 MVP，或发布前必须升级 ERA5/ERA5-Land。
- 是否采用分支 + Pull Request 审核；当前仍按明确指令直接提交 `main`。

## 风险与阻塞

- 六十甲子纪日锚点仍缺第二个独立权威来源；发布前应扩展跨年代回归。
- 节气分钟边界当前只覆盖 2024 立春与惊蛰；后续升级候选库时必须扩样。
- GeoNames 坐标可能有重名或质量问题，需保存快照、署名并提供确认/纠错通道。
- 真太阳时在时辰边界 60 秒内保留两盘；USNO 方案只支持 1800–2200。
- 晚子时与大运是明确的产品口径，不是所有流派共同结论；说明页必须披露。
- 身强弱、格局和喜用神的框架已定，但具体权重仍需实现期回归和人工抽查。
- 城市五行没有可观察的科学金标；传统资料只能支持高层语义，具体字段、权重和公式属于现代产品设计。
- 100 城自然事实齐全，但 NASA POWER 气候/辐射网格较粗，因此数据置信度统一为 Medium；如产品承诺城市内部差异，需升级更高分辨率数据。
- v0.1 中火、金均没有任何城市成为最高维度，土在高海拔四分位中占 84%；模型仍有结构偏差。
- 原始余弦均值 0.9771、P90 0.9976，会把自然差异显著的城市包装成近乎相同；在替换距离口径前不能直接展示为“匹配百分比”。
- 现代辅助四字段没有全球统一可比数据，当前 100 城全部缺失并中性化；80/20、85/15、90/10 结果只证明中性收缩稳定，不能代表真实辅助层影响。

## 更新要求

每个 Work 任务结束前，必须更新：

1. 对应领域正式文件或研究文件；
2. 本文件的日期、最近完成、正在进行和下一步；
3. `TASKS.md` 的任务状态与结果链接；
4. 如产生经 Chat 确认的新决策，再更新 `DECISIONS.md`。

如果任务没有形成可提交成果，也要在本文件记录原因和阻塞，不得只留在聊天中。
