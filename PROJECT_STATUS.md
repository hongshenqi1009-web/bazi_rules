# PROJECT STATUS

- 项目：五行城市
- 仓库：`hongshenqi1009-web/bazi_rules`
- 最后更新：2026-09-10
- 当前阶段：T-003 确定性计算金标测试第一阶段
- 状态：进行中
- 本文件负责人：每次完成任务的 Work 执行者

## 当前目标

在已确认工程边界内，为节气、时区、DST、晚子时、真太阳时与大运起运建立可重复、可追溯的确定性测试；没有独立权威来源的项目必须明确保留为候选。

## 最近完成

- 建立项目目录骨架。
- 建立三端角色分工和标准工作流。
- 建立正式规则与候选研究的审核隔离。
- 建立状态、任务和决策台账。
- 完成三个固定候选提交的 README、许可证、核心源码、测试和算法入口审计；原生测试记录为 `lunar-javascript` 296 项、`zhiji-bazi` 15 项、`tianji` 140 项通过。
- Chat 批准 D-005 至 D-008：候选底座分工、真太阳时默认行为、大运时间精度及争议解释输出结构。
- 建立 T-003 第一阶段测试数据，加入节气分钟边界、IANA 时区、DST gap/fold、海外出生、晚子时、真太阳时跨时辰及大运候选案例。

## 正在进行

- 扩充独立历法/天文金标，并将四柱与大运候选结果从“已记录”提升为可独立验收的断言。

## 下一步

1. 为不少于 12 个节气建立独立到分钟的前后边界样本，不能只覆盖立春。
2. 选定独立的六十甲子纪日核验来源，验证日柱与晚子时两种一致口径。
3. 为大运顺逆及起运折算寻找可引用、可复算且独立于候选库的规则来源。
4. 将测试框架接入未来的适配器自动化测试；当前不开发前端。

## 待 Chat 决定

- 晚子时默认按同日还是次日；混合兼容模式已排除。
- 大运最终采用的顺逆与起运折算口径。
- 真太阳时生产算法、精度及经纬度数据源。
- 是否采用分支 + Pull Request 审核；本轮按明确指令直接提交 `main`。

## 风险与阻塞

- HKO 可独立验证节气事件时刻，但不直接证明子平术的年/月柱切换口径。
- IANA/政府资料可以验证时区与 DST，不能验证四柱或大运。
- 晚子时与大运起运仍缺独立金标；候选库输出只记录为观察值。
- `lunar-javascript` 的默认晚子时路径会产生已排除的混合口径，适配器必须显式拦截。

## 更新要求

每个 Work 任务结束前，必须更新：

1. 对应领域正式文件或研究文件；
2. 本文件的日期、最近完成、正在进行和下一步；
3. `TASKS.md` 的任务状态与结果链接；
4. 如产生经 Chat 确认的新决策，再更新 `DECISIONS.md`。

如果任务没有形成可提交成果，也要在本文件记录原因和阻塞，不得只留在聊天中。
