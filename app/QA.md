# T-008 移动端纵向切片验收记录

- 验收日期：2026-09-13
- 视口：390 × 844 CSS px
- 浏览器：Microsoft Edge（Chromium，无头模式）
- 数据边界：`t008-demo` 结构样板；未冒充正式 BaZi / Matching / City Profile 服务

## 自动化结果

- `node --check`：`dev-server.mjs`、应用入口、样板数据、校验器、分享卡与服务适配器均通过。
- `node --test`：9/9 通过，覆盖输入、23:00 子时分段、标准地点、请求合同、异步阶段顺序、Top 3 顺序、分享卡 3:4 比例及出生信息隔离。
- 浏览器控制台：0 error。
- 横向溢出：`scrollWidth = clientWidth = 390`。
- 本地服务器：首页 `/` 返回 200；未知前端路径回退 `index.html`。

## 纵向流程走查

测试输入：`2000-10-09 · 未时 · 青岛 · 女`。

1. 首页标题、副标题与“开始探索”进入 Step 1。
2. Step 1 可选择日期、十二时辰、精确时间次级入口和“不确定”；子时要求补选 23:00–23:59 / 00:00–00:59 / 不确定。
3. Step 2 只接受标准地点项并要求性别；摘要显示 `2000.10.09 · 未时 · 青岛`。
4. 推演页由真实 Promise 状态依次显示 `bazi → matching → content → complete`，不使用虚假百分比或固定倒计时。
5. 结果一先出现无百分比五行画像，再经星轨纵向展开到城市能量与下一步 CTA。
6. 结果二按样板响应原序显示 01 / 02 / 03，不在前端重排或修改分数。
7. 城市详情显示城市名、契合指数、标签、媒体占位、城市特色、为什么契合和感受；样板文案明确标注待事实复核。
8. 分享预览显示 Top 1、Top 2、Top 3 与契合指数；不含出生日期、时辰、地点、性别或四柱。
9. SVG 下载输出 1080 × 1440；二维码明确为不可扫描的结构占位，正式入口接入后替换。

## 视觉验收

- 移动端保持墨绿黑、暗金、细线、留白和纵向仪式感。
- 城市与 Logo 均使用品牌占位资产；没有将未授权素材写入仓库。
- 二维码占位采用暗底金色模块和星轨圆环，不出现突兀白底。
- `prefers-reduced-motion` 下关闭长动效并直接显示结果内容。
- 跳转主内容链接默认离屏，只在键盘焦点可见。

## 尚未纳入 T-008 的发布项

- 正式 BaZi / Interpretation / Matching 服务与全局地点搜索；
- 经事实及许可审核的 City Profile、城市图片与 Logo 源文件；
- 真实二维码、结果持久化、部署、监控、隐私政策及数据保留机制；
- 主流真机与网络异常矩阵。这些应在真实服务接入与发布准备任务中完成。

## T-009 真实链复验

- 复验日期：2026-09-15
- 视口：390 × 844 CSS px
- 浏览器：Codex in-app browser（Chromium）
- 测试输入：`2000-10-09 · 未时 · 青岛 · 女`
- 地点：输入中文“青岛”后返回`geonames:1797929`、Shandong、中国、`Asia/Shanghai`与标准经纬度；未展示技术字段。
- 四柱：服务端得到`庚辰 · 丙戌 · 庚子 · 癸未`；前端没有相应算法或常量。
- 结果：真实链返回“水木相生型”，Top 3 为 Auckland、Seattle、New Orleans，三项契合指数均为 95；同分顺序由服务端稳定返回，前端未重排。
- AI 降级：本机未配置 AI 凭据；城市详情保留名称、指数、标签与图片占位，明确显示“详细解读暂时不可用”，没有显示 demo 文案。
- 分享：分享卡不含出生日期、时辰、地点、性别或四柱；二维码为真实可扫描数据图，只指向配置的公开入口。
- 隐私：Step 2 提交区直接说明不注册、不长期保存和 15 分钟删除，并可打开独立隐私说明页。
- 视觉：无横向布局溢出；手机竖屏的首页、两步输入、结果一、Top 3、详情和分享预览均可操作。

### 自动化

- `service`: 11/11 通过，覆盖生产 HTTPS 配置、非整点时区、DST gap/fold、四柱、23:00 换日、子时双候选、AI 脱敏/结构/失败隔离、地点与 City Profile、完整真实链。
- `app`: 12/12 通过，覆盖真实地点/API 适配、服务失败不回退 demo、输入合同、状态顺序、Top 3 顺序和分享卡出生信息隔离。
- 浏览器控制台：0 error。

### 尚待上线环境验收

- 使用真实生产 AI 凭据完成 8 城至少各一例内容验收；
- 注入最终`.com`并用手机扫描分享二维码；
- 替换获授权 Logo 与城市主图；
- 香港发布后完成中国移动/联通/电信与海外网络测试、限流和告警演练。
